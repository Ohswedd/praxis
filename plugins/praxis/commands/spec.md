---
description: Restructure a vague request into an explicit engineering spec before any work.
argument-hint: "<the request to turn into a spec>"
---

Use the `prompt-architect` skill to turn this into a precise spec:

> ${ARGUMENTS}

Produce goal, in-scope, out-of-scope/non-goals, acceptance criteria, assumptions,
and open questions. Surface ambiguity and any scope you'd exclude — never narrow it
silently. Ask only genuinely blocking questions; otherwise state the assumption
you'd proceed under. Keep it proportional to the task.
